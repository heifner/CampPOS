project : CPPBase {
  includes += $(BSAPI_ROOT)/include
  libpaths += $(BSAPI_ROOT)/lib/32-bit
  
  includes += $(SQLITE_ROOT)
  libpaths += $(SQLITE_ROOT)
  
  pure_libs += bsapi.lib
  pure_libs += sqlite3.lib
  
  managed = 1
}
